<UserControl x:Class="VegaBeamTool.CameraUI.CoordinateRecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VegaBeamTool.CameraUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:DataGridRowToIndexNumConverter x:Key="DataGridRowToIndexNumConverter"/>
    </UserControl.Resources>
    <Grid>
        <ScrollViewer x:Name="RecordDataScrollView" >
            <DataGrid x:Name="RecordDataGrid" AutoGenerateColumns="False" ItemsSource="{Binding RecordDataTable}"  MouseRightButtonDown="DataGrid_MouseRightButtonDown">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Delete" Click="DataGrid_DeleteItmeClick"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.CellStyle>
                    <Style BasedOn="{StaticResource {x:Type DataGridCell}}" TargetType="{x:Type DataGridCell}">
                        <Setter Property="BorderBrush" Value="Black" />
                        <Setter Property="BorderThickness" Value="0.1" />
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn  Width="50" Binding="{Binding SNoIndex}"  IsReadOnly="True" Header="SNo" />
                    <DataGridTemplateColumn Header="Color">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding CenterColor}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Width="*" Binding="{Binding CenterX}" IsReadOnly="True" Header="CenterX"/>
                    <DataGridTextColumn Width="*" Binding="{Binding CenterY}" IsReadOnly="True" Header="CenterY"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>
